import {notFound} from 'next/navigation';
import {areas,standardItems} from '@/lib/content';
import {PageIntro,CTA,CheckList} from '@/components/ui';
export function generateStaticParams(){return areas.map(a=>({slug:a.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=areas.find(a=>a.slug===slug);return {title:`House Cleaning in ${a?.name||'New Bern'}, NC`,description:a?.description};}
export default async function Area({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const area=areas.find(a=>a.slug===slug);if(!area)notFound();return <><PageIntro label={`${area.name}, North Carolina`} title={`Your ${area.name} home, handled.`}>{area.description}</PageIntro><section className="container content-section split"><div><h2>See your price.<br/>Make a little time.</h2><p>Tell us your home size, choose Standard, Deep or Move cleaning, then add the extras that make your day easier. Available times and your complete price are shown before payment.</p><CTA/></div><div className="panel"><h3 style={{marginBottom:20}}>The everyday reset includes</h3><CheckList items={standardItems}/><p className="small-text" style={{marginTop:25}}>Availability depends on your ZIP, home and selected date. Homes over 3,500 sq ft and unusual conditions require a personal review.</p></div></section></>;}
