import {AdminSetup} from '@/components/admin-setup';
export const metadata={title:'Set up Control Room access',robots:{index:false,follow:false},referrer:'no-referrer' as const};
export default function Setup(){return <AdminSetup/>;}
