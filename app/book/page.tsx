import {defaultConfig} from '@/lib/pricing';import {defaultScheduling} from '@/lib/scheduling';import {BookingFlow} from '@/components/booking-flow';export default async function Book({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){return <BookingFlow config={defaultConfig} scheduling={defaultScheduling} initial={await searchParams} leadReady={true}/>;}