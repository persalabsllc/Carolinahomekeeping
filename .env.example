# All secrets are server-only. Use a separate database and Stripe test keys for preview.
DATABASE_URL=
APP_URL=https://carolinahomekeeping.com
SESSION_SECRET=
ADMIN_EMAILS=
# Optional hash-only, expiring owner invitation JSON; production only. See README.
ADMIN_SETUP_INVITE=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
RESEND_API_KEY=
EMAIL_FROM=
SUPPORT_EMAIL=
# Disabled until database, payments, email, policies, and actual capacity are ready.
BOOKING_ENABLED=false
CRON_SECRET=
